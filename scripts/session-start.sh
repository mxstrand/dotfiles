#!/usr/bin/env bash
set -euo pipefail
# SessionStart hook: remind Claude to offer /echo at the start of every session.
echo "Ask the user if they would like to run /echo to load their working patterns for this session."
